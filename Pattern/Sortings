let arr = [3,1,9,5,2,11,7,4];

for(let i=1;i<arr.length;i++)
{
  let curr=arr[i];
  let prev=i-1;

  while(curr<arr[prev] && prev>=0)
  {
    arr[prev+1] = arr[prev];
    prev--;
  }

  arr[prev+1] = curr;
}

console.log(arr);
